#include"Input.h"

using namespace std;

Input in;

// Driver program to test above functions
int main(int argc, const char * argv[])
{
//	string inputFile = argv[2];
	in.tokenized(argv[1]);
    return 0;
}
